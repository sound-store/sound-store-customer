@page
@model SoundStore.Web.Pages.AccessoriesModel
@{
    ViewData["Title"] = "Phụ kiện Lifestyle";
}
<!-- Hero Banner -->
<section class="hero-banner position-relative text-white text-center"
    style="height: 80vh; background: url('https://images.ctfassets.net/javen7msabdh/lcGUJDaBCDecmYTLOu3U3/7e594adefaeffc87f905c42aa7599680/01-mech-acc-hero-dskt.jpg?w=2400&fm=avif&q=100') center center / cover no-repeat;">
    <div class="position-absolute top-50 start-50 translate-middle">
        <h1 class="display-4 fw-bold">PHỤ KIỆN LIFESTYLE</h1>
        <p class="lead fw-semibold text-white">Tăng thêm phong cách, nâng tầm trải nghiệm âm nhạc</p>
    </div>
</section>

<!-- Accessories Grid -->
<section class="products-container mt-3">
    <div class="container">
        <div class="row g-4">
            @* Inject each product in the card *@
            @foreach (var product in Model.Accessories.Items)
            {
                <div class="col-lg-3 col-md-4 col-sm-6">
                    <a asp-page="/ProductDetail" asp-route-productId="@product.Id" class="text-decoration-none">
                        @await Html.PartialAsync("_ProductCard", product)
                    </a>
                </div>
            }
        </div>

        <!-- Pagination (Simple Buttons) -->
        @* 
            @if (Model.Speakers.TotalPages > 1)
            {
                <div class="d-flex justify-content-center">
                    <nav>
                        <ul class="pagination">
                            @if (Model.Speakers.HasPreviousPage)
                            {
                                <li class="page-item">
                                    <a class="page-link"
                                       asp-page="/Speakers"
                                       asp-route-categoryId="@Request.Query["categoryId"]"
                                       asp-route-pageNumber="@(Model.Speakers.PageNumber - 1)">Previous</a>
                                </li>
                            }

                            @for (int i = 1; i <= Model.Speakers.TotalPages; i++)
                            {
                                <li class="page-item @(i == Model.Speakers.PageNumber ? "active" : "")">
                                    <a class="page-link"
                                       asp-page="/Speakers"
                                       asp-route-categoryId="@Request.Query["categoryId"]"
                                       asp-route-pageNumber="@i">@i</a>
                                </li>
                            }

                            @if (Model.Speakers.HasNextPage)
                            {
                                <li class="page-item">
                                    <a class="page-link"
                                       asp-page="/Speakers"
                                       asp-route-categoryId="@Request.Query["categoryId"]"
                                       asp-route-pageNumber="@(Model.Speakers.PageNumber + 1)">Next</a>
                                </li>
                            }
                        </ul>
                    </nav>
                </div>
            }
        *@
    </div>
</section>